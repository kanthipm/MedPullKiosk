# Dev-only: OpenAI API key for browser testing outside the kiosk WebView.
# In production the kiosk Android bridge uses BuildConfig.GROK_API_KEY / GROQ_API_KEY.
# Enter this via the settings gear icon in the browser dev build.
VITE_OPENAI_API_KEY=sk-...
