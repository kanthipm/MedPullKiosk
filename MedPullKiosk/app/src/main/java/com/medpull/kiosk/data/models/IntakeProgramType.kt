package com.medpull.kiosk.data.models

/**
 * The type of intake program the patient is starting.
 * Drives which form schema is loaded and which flow is entered.
 */
enum class IntakeProgramType {
    SLIDING_FEE,
    MEDICAL_INTAKE
}
